# Copy this file to credentials.py and fill in your Spotify app credentials.
# Do NOT commit the filled credentials.py (already ignored by .gitignore).

CLIENT_ID = "your_spotify_client_id"
CLIENT_SECRET = "your_spotify_client_secret"
